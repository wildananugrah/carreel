{
  "name": "carreel-driver-database",
  "private": true,
  "scripts": {
    "migrate:dev": "bunx prisma migrate dev",
    "migrate:deploy": "bunx prisma migrate deploy",
    "generate": "bunx prisma generate && bun run scripts/post-generate.ts",
    "studio": "bunx prisma studio",
    "seed": "bunx prisma db seed"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.0.0",
    "@prisma/client": "^7.0.0",
    "dotenv": "^17.3.1",
    "prisma": "^7.0.0"
  }
}
